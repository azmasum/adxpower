{
  "name": "adxpower",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "bootstrap": "npm install",
    "build:server": "cd server && npx prisma generate && npx nest build",
    "start:server": "cd server && npx prisma migrate deploy && node dist/main.js"
  },
  "dependencies": {
    "7zip-bin": "^5.2.0",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.2.0"
  }
}
